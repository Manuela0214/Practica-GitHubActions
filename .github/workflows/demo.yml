name: Demo github action

on:
  push:
    branches:
      - main

jobs:
  demo_trabajo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Demo 1 de GitHub Actions"


